# FlashCardGenerator

This project requires Python version 3.12.12.

Please ensure you have Python 3.12.12 installed before creating or activating the virtual environment.

To create the virtual environment:

```bash
python3.12 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
